apiVersion: v1
kind: Service
metadata:
  name: observability-dashboard
  namespace: observability-dashboard
  labels:
    app: observability-dashboard
spec:
  selector:
    app: observability-dashboard
  ports:
    - port: 8000
      targetPort: 8000
